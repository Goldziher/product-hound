export enum HttpMethod {
	Delete = 'DELETE',
	Get = 'GET',
	Patch = 'PATCH',
	Post = 'POST',
	Put = 'PUT',
}

export enum HttpStatus {
	Forbidden = 403,
	BadRequest = 400,
	InternalServerError = 500,
	OK = 200,
}
